# local runtime output (config + logs live in %APPDATA%, but just in case)
config.json
events.log
*.log
.version

# ntfy server secrets - never commit
ntfy-server/.env
ntfy-server/monitor.token
ntfy-server/monitor.token.raw
